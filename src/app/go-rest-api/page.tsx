import RestApi from "@/pages/home/RestApi"

export const metadata = {
    title: 'API REST con CRUD en Go | Learning Go',
    description: 'Aprende a construir una API REST completa con operaciones CRUD y JSON en Go.',
}

function GoRestApi() {
    return (
        <RestApi />
    )
}

export default GoRestApi
